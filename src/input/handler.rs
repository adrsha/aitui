// Placeholder — will be implemented in Phase 2.
// This file exists so the crate compiles while we build the UI layer first.
pub fn handle_event(_app: &crate::app::App, _event: crossterm::event::Event) -> Vec<crate::app::Action> {
    vec![]
}
